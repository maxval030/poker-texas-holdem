# Made hand assist is a local viewer toggle

Made hand label and Contributing cards highlight follow a per-viewer Made hand assist preference (on/off), stored in browser localStorage, defaulting to on on first visit. We rejected a permanent always-visible coaching HUD so players can clear the felt, rejected server-stored prefs for v1, and kept Hand chart on a separate control so reference material stays available when the HUD is off.
